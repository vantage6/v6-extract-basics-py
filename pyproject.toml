[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "v6-extract-basics-py"
version = "5.0.1"
description = "This algorithm makes the extraction functions defined in the vantage6 algorithm tools available in an algorithm image."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "vantage6-algorithm-tools>=5.0.1",
  "pandas",
]
authors = [
  { name = "Bart van Beusekom", email = "b.vanbeusekom@iknl.nl" },
]
license = { text = "MIT" }
keywords = ["vantage6", "algorithm", "federated-learning"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.13",
]

[project.urls]
Homepage = "https://github.com/vantage6/v6-extract-basics-py"
Repository = "https://github.com/vantage6/v6-extract-basics-py"
Issues = "https://github.com/vantage6/v6-extract-basics-py/issues"

[tool.hatch.build.targets.wheel]
packages = ["v6-extract-basics-py"]

[tool.uv]
prerelease = "allow"